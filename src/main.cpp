#include "main.hpp"

int main(void)
{
    aspirateur *aspi = new aspirateur();
    aspi->boucle();
    // aspi->getResult();
    // std::string value;
    // std::cin >> value;
    // std::cout << value << std::endl;

    return (0);
}
